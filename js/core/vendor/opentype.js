/**
 * opentype.js bundled from node_modules. Single import site replaces the
 * former global <script> tag; used by the SVG/PNG text-to-path export path.
 */
import opentype from 'opentype.js';
export { opentype };
